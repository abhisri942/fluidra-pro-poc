name: 'fluidra_pro'
version: '1.0.0'

profile: 'fluidra_pro'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  fluidra_pro:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:;
      dimensions:
        +schema: dimensions
        +materialized: table
      facts:
        +schema: facts
        +materialized: view
